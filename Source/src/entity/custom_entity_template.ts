import { CustomEntityData } from "../types";

export const customEntityDataTemplate: Partial<CustomEntityData> = {
    // _callbacksPerTick: new Set(),
    // _persistentData: {
    //     placeholder: null,
    // },
    //_persistentDataSaveScheduled: false,
    _tempData: {
        placeholder: null
    }
};